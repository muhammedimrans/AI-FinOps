# Provider Adapters (workspace packages)

**Status: Planned — no adapter package code exists yet.**

Standalone adapter packages are planned for a later phase. The adapters in
production use today live in the backend at `backend/app/providers/adapters/`
(OpenAI, Anthropic, Azure OpenAI, Google, Grok, Ollama, OpenRouter).
