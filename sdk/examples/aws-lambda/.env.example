# Set as Lambda environment variables (console, SAM template, CDK, or
# Serverless Framework config), not a .env file loaded at runtime.
COSTORAH_API_KEY=costorah_live_xxxxxxxxx
COSTORAH_ENDPOINT=https://api.costorah.com
OPENAI_API_KEY=sk-your-openai-key-here
