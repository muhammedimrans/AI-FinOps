# Development defaults — committed, no secrets.
# Override locally with .env.development.local (gitignored).

VITE_API_BASE_URL=http://localhost:8000

# Enable mock data in development so the app runs without a live backend.
# Set to false (or remove) to test against the real backend.
VITE_ENABLE_MOCKS=true
