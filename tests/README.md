# Cross-service test suites

**Status: Planned — scaffolding only.**

Intended for E2E (Playwright), load, performance, and security suites that span
services. Unit/integration tests that exist today live in `backend/tests/`
(1000+ tests) and `frontend/src/__tests__/`.
