# (superseded)

This placeholder is superseded by [`sdk/javascript/`](../javascript/), which
holds the real `@costorah/sdk` package (EP-18.1). Kept as an empty
redirect rather than deleted, since an external link or fork may still
reference this path.
